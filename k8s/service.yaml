apiVersion: v1
kind: Service
metadata:
  name: shopsmart-frontend-svc
  namespace: shopsmart-prod
spec:
  type: LoadBalancer
  selector:
    app: shopsmart-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
